---
import BaseLayout from './BaseLayout.astro';
import TagBadge from '../components/TagBadge.astro';

const { title, seoTitle, description, date, tags, image, body = '' } = Astro.props;
const readingTime = Math.max(1, Math.round(body.split(/\s+/).filter(Boolean).length / 220));
const imageUrl = image ? new URL(image, Astro.site).toString() : undefined;
const canonicalUrl = Astro.url.href;
const blogPostingSchema = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: seoTitle ?? title,
  name: title,
  description,
  image: imageUrl,
  datePublished: date.toISOString(),
  dateModified: date.toISOString(),
  author: {
    '@type': 'Person',
    name: 'MadDogWarner',
    url: new URL('/about/', Astro.site).toString(),
  },
  publisher: {
    '@type': 'Person',
    name: 'MadDogWarner',
    url: new URL('/', Astro.site).toString(),
  },
  keywords: tags.join(', '),
  url: canonicalUrl,
  mainEntityOfPage: {
    '@type': 'WebPage',
    '@id': canonicalUrl,
  },
};
---

<BaseLayout title={title} seoTitle={seoTitle} description={description} image={image} structuredData={blogPostingSchema}>
  <div class="progress" data-progress></div>
  <article class="container post">
    <p class="breadcrumb mono"><a href="/">Home</a> &gt; <a href="/blog/">Blog</a> &gt; {title}</p>
    <h1>{title}</h1>
    <div class="meta">
      <time datetime={date.toISOString()}>{date.toLocaleDateString('en-AU', { day: '2-digit', month: 'long', year: 'numeric' })}</time>
      <span>{readingTime} min read</span>
    </div>
    <div class="tags">{tags.map((tag: string) => <TagBadge tag={tag} />)}</div>
    {image && <img class="post-image" src={image} alt="" loading="eager" decoding="async" />}
    <div class="prose content">
      <slot />
    </div>
    <footer class="author">Written by MadDogWarner | Assisted by Claude & Codex</footer>
  </article>
  <script>
    const progress = document.querySelector('[data-progress]');
    window.addEventListener('scroll', () => {
      const max = document.documentElement.scrollHeight - window.innerHeight;
      const pct = max > 0 ? (window.scrollY / max) * 100 : 0;
      progress?.setAttribute('style', `width: ${pct}%`);
    }, { passive: true });
  </script>
</BaseLayout>

<style>
  .progress {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 100;
    height: 3px;
    background: var(--color-accent);
  }

  .post {
    padding: 4rem 0 6rem;
  }

  .breadcrumb {
    color: var(--color-muted);
    font-size: 0.82rem;
  }

  .breadcrumb a {
    color: var(--color-accent);
  }

  h1 {
    margin: 0.7rem 0;
    color: #fff;
    font-family: "Space Grotesk", sans-serif;
    font-size: clamp(2.5rem, 6vw, 4.8rem);
    line-height: 1;
  }

  .meta,
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
  }

  .meta {
    color: var(--color-muted);
  }

  .content {
    margin-top: 2rem;
  }

  .content :global(p) {
    margin: 1.15rem 0;
  }

  .content :global(h2) {
    margin: 3.25rem 0 1.1rem;
    padding-top: 0.35rem;
    border-top: 1px solid rgba(0, 180, 255, 0.18);
    font-size: clamp(1.65rem, 4vw, 2.35rem);
  }

  .content :global(h3) {
    margin: 2.35rem 0 0.85rem;
    font-size: clamp(1.3rem, 3vw, 1.75rem);
  }

  .content :global(h2 + p),
  .content :global(h3 + p) {
    margin-top: 0;
  }

  .content :global(ul),
  .content :global(ol) {
    margin: 1.25rem 0 1.6rem;
    padding-left: 1.35rem;
  }

  .content :global(li + li) {
    margin-top: 0.55rem;
  }

  .post-image {
    display: block;
    width: min(100%, 32rem);
    aspect-ratio: 1;
    margin: 2rem 0 0;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    object-fit: cover;
  }

  .author {
    margin-top: 3rem;
    border-top: 1px solid var(--color-border);
    padding-top: 1.25rem;
    color: var(--color-muted);
  }

  @media (max-width: 640px) {
    .post {
      padding-top: 3rem;
    }

    .content :global(h2) {
      margin-top: 2.6rem;
    }

    .content :global(h3) {
      margin-top: 2rem;
    }
  }
</style>
